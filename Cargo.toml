[workspace]
members = ["crates/rctool-core", "crates/rctool-cli", "apps/tray/src-tauri"]
resolver = "2"

[workspace.package]
version = "0.1.0"
edition = "2021"
license = "GPL-3.0-only"

[profile.release]
lto = "thin"
strip = true
